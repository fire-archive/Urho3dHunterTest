cmake_minimum_required(VERSION 3.2.3)
project(UrhoHunterTest)
if (COMMAND cmake_policy)
    cmake_policy (SET CMP0003 NEW)
    if (CMAKE_VERSION VERSION_GREATER 2.8.12 OR CMAKE_VERSION VERSION_EQUAL 2.8.12)
        # INTERFACE_LINK_LIBRARIES defines the link interface
        cmake_policy (SET CMP0022 NEW)
    endif ()
    if (CMAKE_VERSION VERSION_GREATER 3.0.0 OR CMAKE_VERSION VERSION_EQUAL 3.0.0)
        # Disallow use of the LOCATION target property - so we set to OLD as we still need it
        cmake_policy (SET CMP0026 OLD)
        # MACOSX_RPATH is enabled by default
        cmake_policy (SET CMP0042 NEW)
    endif ()
endif ()
set(CMAKE_MODULE_PATH ${CMAKE_SOURCE_DIR}/CMake/Modules;${CMAKE_MODULE_PATH})
include(UrhoCommon)
if(WIN32)
  install(DIRECTORY "${CMAKE_BINARY_DIR}/share/Resources/Autoload" DESTINATION share/Resources)
  install(DIRECTORY "${CMAKE_BINARY_DIR}/share/Resources/CoreData" DESTINATION share/Resources)
  install(DIRECTORY "${CMAKE_BINARY_DIR}/share/Resources/Data" DESTINATION share/Resources)
else()
  install(DIRECTORY "${CMAKE_BINARY_DIR}/share/Urho3D/Resources/Autoload" DESTINATION share/Urho3D/Resources)
  install(DIRECTORY "${CMAKE_BINARY_DIR}/share/Urho3D/Resources/CoreData" DESTINATION share/Urho3D/Resources)
  install(DIRECTORY "${CMAKE_BINARY_DIR}/share/Urho3D/Resources/Data" DESTINATION share/Urho3D/Resources)
endif()
if(WIN32)
install(FILES 42_PBRMATERIALS/.itch.toml DESTINATION .)
install(FILES ${CMAKE_BINARY_DIR}/bin/Urho3DPlayer.exe DESTINATION bin)
install(FILES ${CMAKE_BINARY_DIR}/bin/Editor.bat DESTINATION bin)
else()
endif()

add_subdirectory(42_PBRMaterials)
